# Gradle properties for Android module (added to ensure CI reads AndroidX settings)
# These are required so builds that run inside the android/ directory see AndroidX enabled
android.useAndroidX=true
android.enableJetifier=true

# JVM args for Gradle
org.gradle.jvmargs=-Xmx1536m
